[package]
name = "tpt-agent-graph"
description = "Thin state-machine/DAG orchestration for LLM agent turns"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
documentation = "https://docs.rs/tpt-agent-graph"
readme = "README.md"
keywords = ["agent", "orchestration", "state-machine", "llm", "tool-use"]
categories = ["asynchronous", "data-structures"]

[features]
default = ["std"]
std = []

[dev-dependencies]
serde_json = { workspace = true, features = ["std"] }
