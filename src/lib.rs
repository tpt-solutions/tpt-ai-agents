//! Workspace-root package with no public API of its own.
//!
//! It exists only to host `examples/`, which demonstrate composing the
//! individual `tpt-*` crates together. See `examples/` and
//! `GETTING_STARTED.md`.
